import { HeroCanvas } from "@/components/effects/HeroCanvas";

export default function Home() {
  return (
    <main className="relative min-h-screen w-full flex flex-col bg-[#080807] overflow-hidden">
      <HeroCanvas />
    </main>
  );
}
