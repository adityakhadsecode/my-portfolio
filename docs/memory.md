# Project Memory & User Constraints — Aditya Khadse Portfolio

- **Hero Background Scope**: The `<ShaderGradient>` (3D animated sphere shader) is strictly scoped to the **Hero Section background** (`HeroCanvas.tsx` inside `HeroSection.tsx`). It must NOT spill over into or wrap subsequent sections (About, Works, Contact, etc.).
- **Theme & Colors**: Dark editorial palette — Base background `#080807`, primary text `#e8e8e3`, accent muted `#8c8c73`.
- **Preloader / Intro**: Intro sequence preloader deferred until after Hero Section approval as requested.
